# Implementation Plans — v0.2.0 Dogfooding Handoff

Generated by an external advisor audit on 2026-07-21 (planned at commit `8716814`).
**Executor: Grok (this repository's Operator), dogfooding the grok-orchestra harness itself.**

## 引継ぎ指示(Grok Operator 向け)

あなたはこのワークスペースの Operator です。ルートの `AGENTS.md` と `.agents/skills/` に従い、
以下の 4 プランを**このハーネス自身の運用ルールで**実行してください。これはドッグフーディングです:
ハーネスの規律(packet → delegate → verify)を守れない場合、それ自体が発見としてレポート対象です。

各プランの実行プロトコル:

1. `context-loader` 相当の最小ロード(AGENTS.md / STATE.md / 対象プランのみ)で開始する。
2. プランを全文読んでから着手する。**Drift check を必ず最初に実行**する。
3. 設計判断が必要な箇所は Codex `read-only` に委譲する(Prompt Contract packet を
   `.agents/docs/packets/plan-NNN-design.prompt.txt` に書き、`scripts/delegate-codex.ps1` 経由)。
4. 実装は Grok(あなた)が行う。デフォルトルーティング通り。
5. 非自明な変更後は Codex `review` packet を必ず通す(AGENTS.md「Done means」参照)。
6. `verify-job` スキルのチェックリストを実行してから DONE を宣言する。
7. 下の Status 表と `.agents/STATE.md` を更新する。
8. プランごとに feature ブランチ + PR(直近の履歴に倣い `feat/...` / `docs/...` 形式、
   コミットメッセージは英語)。push / PR 作成前にユーザーへ確認する。

**STOP 共通条件**: 各プランの STOP conditions に加え、同一の失敗が 2 回続いたら
サーキットブレーカー(AGENTS.md F05/F17)を発動し、第 3 の変種を試さずユーザーへ報告する。

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Pester テスト + GitHub Actions CI 基盤 | P1 | S–M | — | DONE |
| 002  | check.ps1(doctor)+ verify-job.ps1 で規約を機構化 | P1 | M | 001 | TODO |
| 003  | install.ps1 ワンコマンド導入 | P2 | M | 001 | TODO |
| 004  | エンドツーエンド walkthrough ドキュメント | P2 | S | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 は 001 に依存: スクリプト変更(delegate-codex.ps1 への PID 記録追加等)を
  回帰テストの傘の下で行うため。
- 003 は 001 に依存: installer の冪等性テストを Pester で書くため。
- 004 は 002 に依存: walkthrough に check.ps1 / verify-job.ps1 の実行例を含めるため。
- 004 は Codex CLI へのログイン済み環境が必要(実トランスクリプトを採取する)。

## Findings considered and rejected

- **L2 worktree ヘルパー(worktree-job.ps1)**: 価値は高いがマージ回収の設計が未確定で、
  雑に作ると F08/F10(二重書き込み・yolo マージ)の入口になる。v0.3 で設計 spike から。
- **ワーカーアダプタ層(delegate-worker.ps1 -Runtime codex|claude|gemini)**: 母集団拡大の
  本命だが、各 CLI の sandbox 意味論の差異吸収が本体で工数 L。コア価値の希釈リスクも
  あるため v0.2 には含めない。
- **コスト/トークン計測(F11 対策の telemetry)**: codex exec の出力からの安定した
  トークン数抽出手段が未確認。手段確認後に再検討。
