# PROGRESS

## 2026-07-21

- Bootstrapped Phase 0–1 scaffold (AGENTS, .agents SSOT, skills, delegate-codex.ps1, failure-modes).
- Smoke `smoke-001` passed: `delegate-codex.ps1` → Codex read-only review → `.agents/logs/codex/smoke-001.last.txt`.
- Commit `76a58bd` bootstrap on local main.
- Phase 2: seven workflow skills (`init`, `startproject`, `plan`, `tdd`, `simplify`, `checkpointing`, `design-tracker`).
- Phase 2: L1 foundation — `scripts/lease-paths.ps1`, lease schema in `isolation.md`, optional `-OwnedPaths` on `delegate-codex.ps1`.
- Operator verify-job + wiring (INDEX / HANDOFF / STATE / DESIGN); public remote push next.
